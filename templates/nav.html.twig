<nav>
	<ul>
		{% for item in items %}
			<li>
				<a href="{{ item.url }}" {% if app.request.getPathInfo() == item.url %} class="active" {% endif %}>
					{{ item.label }}
				</a>
			</li>
		{% endfor %}
	</ul>
</nav>

